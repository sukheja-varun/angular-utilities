import { ReplaceCharacterPipe } from './replace-character.pipe';

describe('ReplaceCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
